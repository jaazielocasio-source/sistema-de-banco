<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="BankSystem.UI.Views.AuditLogView">
  <StackPanel Spacing="8">
    <TextBlock Text="Auditoría" FontSize="20" FontWeight="Bold"/>
    <StackPanel Orientation="Horizontal" Spacing="8">
      <TextBlock Text="Filtro"/>
      <TextBox Width="200" Text="{Binding Filter}"/>
      <Button Content="Refrescar" Command="{Binding RefreshCommand}"/>
    </StackPanel>
    <TextBox Text="{Binding LogContent}" AcceptsReturn="True" IsReadOnly="True" Height="420" TextWrapping="Wrap"/>
  </StackPanel>
</UserControl>
