<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="BankSystem.UI.Views.AdminView">
  <StackPanel Spacing="24">
    <!-- Header -->
    <StackPanel Spacing="8">
      <TextBlock Text="Panel de Administración" FontSize="32" FontWeight="Bold" Foreground="#1E293B"/>
      <TextBlock Text="Gestiona clientes, cuentas y configuraciones" FontSize="14" Foreground="#64748B"/>
    </StackPanel>

    <!-- Create Customer Card -->
    <Border Classes="card">
      <StackPanel Spacing="16">
        <StackPanel Orientation="Horizontal" Spacing="12">
          <Border Background="#DBEAFE" CornerRadius="8" Width="40" Height="40" Padding="8">
            <TextBlock Text="👤" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
          </Border>
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Crear Cliente" FontSize="18" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Registra un nuevo cliente en el sistema" FontSize="12" Foreground="#64748B"/>
          </StackPanel>
        </StackPanel>
        
        <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto">
          <TextBox Grid.Row="0" Grid.Column="0" Watermark="Nombre completo" Text="{Binding Name}"/>
          <TextBox Grid.Row="0" Grid.Column="1" Watermark="Email" Text="{Binding Email}"/>
          <TextBox Grid.Row="1" Grid.Column="0" Watermark="Teléfono ###-###-####" Text="{Binding Phone}"/>
          <TextBox Grid.Row="1" Grid.Column="1" Watermark="ID Gobierno" Text="{Binding GovId}"/>
        </Grid>
        
        <Button Classes="primary" Content="➕ Crear Cliente" Command="{Binding CreateCustomerCommand}" HorizontalAlignment="Left" Padding="24,12"/>
      </StackPanel>
    </Border>

    <!-- Create Account Card -->
    <Border Classes="card">
      <StackPanel Spacing="16">
        <StackPanel Orientation="Horizontal" Spacing="12">
          <Border Background="#FEF3C7" CornerRadius="8" Width="40" Height="40" Padding="8">
            <TextBlock Text="💳" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
          </Border>
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Crear Cuenta Bancaria" FontSize="18" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Abre una nueva cuenta para un cliente" FontSize="12" Foreground="#64748B"/>
          </StackPanel>
        </StackPanel>

        <Grid ColumnDefinitions="*,*,*" RowDefinitions="Auto,Auto">
          <StackPanel Grid.Row="0" Grid.Column="0" Spacing="8" Margin="0,0,8,0">
            <TextBlock Text="Cliente ID" FontWeight="Medium" FontSize="12" Foreground="#64748B"/>
            <NumericUpDown Minimum="1" Maximum="999999" Value="{Binding CustomerIdForAccount}" Height="40" CornerRadius="8"/>
          </StackPanel>
          
          <StackPanel Grid.Row="0" Grid.Column="1" Spacing="8" Margin="8,0,8,0">
            <TextBlock Text="Tipo de Cuenta" FontWeight="Medium" FontSize="12" Foreground="#64748B"/>
            <ComboBox SelectedValue="{Binding AccountType}" SelectedValueBinding="{Binding Tag}" Height="40" CornerRadius="8">
              <ComboBoxItem Content="💰 Ahorro" Tag="1"/>
              <ComboBoxItem Content="💵 Corriente" Tag="2"/>
              <ComboBoxItem Content="📊 Certificado de Depósito" Tag="3"/>
            </ComboBox>
          </StackPanel>
          
          <StackPanel Grid.Row="0" Grid.Column="2" Spacing="8" Margin="8,0,0,0">
            <TextBlock Text="Moneda" FontWeight="Medium" FontSize="12" Foreground="#64748B"/>
            <ComboBox SelectedValue="{Binding AccountCurrency}" SelectedValueBinding="{Binding Tag}" Height="40" CornerRadius="8">
              <ComboBoxItem Content="🇺🇸 USD" Tag="USD"/>
              <ComboBoxItem Content="🇪🇺 EUR" Tag="EUR"/>
              <ComboBoxItem Content="🇯🇵 JPY" Tag="JPY"/>
              <ComboBoxItem Content="🇰🇷 KRW" Tag="KRW"/>
              <ComboBoxItem Content="🇧🇴 BOB" Tag="BOB"/>
              <ComboBoxItem Content="🇬🇧 GBP" Tag="GBP"/>
            </ComboBox>
          </StackPanel>
        </Grid>
        
        <Button Classes="primary" Content="➕ Crear Cuenta" Command="{Binding CreateAccountCommand}" HorizontalAlignment="Left" Padding="24,12"/>
      </StackPanel>
    </Border>

    <!-- Update Account Status Card -->
    <Border Classes="card">
      <StackPanel Spacing="16">
        <StackPanel Orientation="Horizontal" Spacing="12">
          <Border Background="#FEE2E2" CornerRadius="8" Width="40" Height="40" Padding="8">
            <TextBlock Text="🔒" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
          </Border>
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Activar / Desactivar Tarjeta" FontSize="18" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Selecciona una tarjeta visualmente para cambiar su estado" FontSize="12" Foreground="#64748B"/>
          </StackPanel>
        </StackPanel>

        <StackPanel Spacing="12">
          <TextBlock Text="📌 Presiona el botón para ver tus tarjetas y elegir cuál activar o desactivar" 
                     FontSize="13" 
                     Foreground="#64748B" 
                     FontStyle="Italic"/>
          
          <StackPanel Spacing="8">
            <TextBlock Text="Nuevo Estado" FontWeight="Medium" FontSize="12" Foreground="#64748B"/>
            <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding NewStatus}" Height="40" CornerRadius="8"/>
          </StackPanel>
        </StackPanel>
        
        <Button Classes="primary" Content="🔍 Elegir Tarjeta y Actualizar" Command="{Binding SetStatusCommand}" HorizontalAlignment="Left" Padding="24,12"/>
      </StackPanel>
    </Border>

    <!-- Status Message -->
    <Border Classes="card" 
            Background="#ECFDF5" 
            BorderBrush="#10B981" 
            BorderThickness="2"
            IsVisible="{Binding StatusMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
      <StackPanel Orientation="Horizontal" Spacing="12">
        <TextBlock Text="✅" FontSize="20"/>
        <TextBlock Text="{Binding StatusMessage}" 
                   FontSize="14" 
                   Foreground="#10B981" 
                   FontWeight="Medium"
                   VerticalAlignment="Center"/>
      </StackPanel>
    </Border>
  </StackPanel>
</UserControl>
